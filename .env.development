# 开发环境配置
# 是否使用本地 Mock 数据（true=后端未就绪时演示用；false=直连后端联调）
VITE_USE_MOCK=true
# 后端接口前缀（Vite 代理会把 /api 转发到 8080 并去掉前缀）
VITE_API_BASE=/api
# 认证（登录/获取档案）是否使用本地 Mock：
# false = 登录走真实后端（其他页面仍可用 Mock 演示）；true = 全部走 Mock
VITE_USE_MOCK_AUTH=false
