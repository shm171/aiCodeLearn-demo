# EduCode 开发日志

> 本文档记录 EduCode 智能学习平台（学员端前端）每一步开发做了什么，方便随时回顾。

---

## 2026-09-15 第十八步：后端核心接口全部完成，等待合并

后端 feature/backend-integration 分支完成，核心功能全部实现：

- 账号与档案：注册、登录、JWT校验、账号与Profile查询/修改/删除
- 源码提交：支持 .java、.cpp、.cc、.cxx，一次请求完成上传与批改
- 双层批改：规则静态检查 + LLM深度批改，返回阶段状态和分数可信度
- 错题归档：列表、详情、分类过滤、标记已掌握
- 学生报告：错题分布、薄弱点、复习清单、月度曲线、正确率
- 教师看板：基本完成（给b队友用）
- 学习助手：SSE流式返回，支持会话ID和JDBC会话记忆
- 未实现：邮箱验证码、找回密码、相似题推荐（扩展项）

数据库表（Flyway迁移）：V1 app_user、V2 profile、V3 角色迁移、V6 source_file、V7 error_record

后端接口一览：
- POST /user/register - 注册
- POST /login - 登录（响应只有token）
- POST /login/validate - 校验token
- GET/PUT /user/{id} - 查询/修改账号
- GET/PUT /user/{id}/profile - 查询/修改档案
- POST /core/submissions - 上传源码并立即返回完整批改结果（一步完成）
- GET /core/review/errors - 分页查询错题，支持category过滤
- GET /core/review/errors/{errorId} - 查询错题详情
- POST /core/review/errors/{errorId}/mastered - 标记已掌握
- GET /core/review/package - 获取学生复习报告
- GET /core/review/weak-topics - 获取薄弱知识点
- GET /core/teacher/dashboard - 教师统计（仅教师/管理员）
- POST /core/assistant/chat - AI学习助手（SSE流式）

前端联调注意事项：
1. 登录响应只有token，用户ID要从注册响应里保存
2. 提交作业只有一步，上传后直接返回完整批改结果
3. gradingResult.status 和 gradingResult.stages 决定成绩是否完整可信
4. accuracyRate 是0.0到1.0的小数，显示百分比时乘以100
5. 错题"已掌握"必须调用后端接口，不能只改本地状态
6. 学习助手使用SSE，X-Conversation-Id响应头需要后续请求原样传回

当前状态：后端分支尚未合并到main，前端暂时继续用模拟数据。

---

## 2026-09-10 第十七步：错题本AI类似题弹窗改左右布局

把AI生成类似题弹窗从"跳转提交页面"改成"弹窗内直接写代码提交"：
- 弹窗宽度从640px加大到1000px，改成左右布局
- 左边：题目信息（标题、描述、考察知识点、代码模板参考，带"填入编辑器"按钮）
- 右边：CodeMirror代码编辑器（带清空按钮）+ "提交并批改"按钮 + 批改结果展示
- 批改结果：得分、整体评语、错误列表（严重程度、类别、行号、错误说明、修改建议）
- 去掉了"去提交练习"按钮，不用跳转页面了
- 翻页时重新初始化编辑器，清空批改结果
- 关闭弹窗时销毁编辑器，释放资源
- 修复了ElMessage提示被弹窗挡住的z-index bug（全局设置z-index: 9999）

---

## 2026-09-09 第十六步：全局样式优化 + 导航栏滚动收缩

全局el-tag样式优化：所有标签改成半透明深色背景 + 浅色文字 + 半透明边框 + 毛玻璃效果

个人中心昵称输入框优化：字数统计平时隐藏，只有输入框聚焦时才显示，去掉白色背景

导航栏滚动收缩效果：
- 导航栏从sticky改成fixed定位（z-index: 100）
- 滚动超过50px时高度从80px缩到60px，字体变小，加0.3s过渡动画
- 修复了弹窗层级bug：去掉.main的z-index，避免弹窗被导航栏挡住

---

## 2026-09-08 第十五步：错题本页面大改

根据用户反馈的6个问题逐一修复：
1. 错题卡片标注语言（C++/Java标签）
2. 详情弹窗显示完整源代码（而非代码片段）
3. 详情增加详细解释、知识点说明（紫色边框高亮）、正确写法示例
4. AI生成类似题有真实题目内容（不再是空模板）
5. 匹配学生提交的语言（C++错题生成C++题，Java错题生成Java题）
6. 每次生成3道题，支持上一题/下一题翻页

模拟错题数据：8个模拟错题，覆盖C++和Java（数组越界、变量未定义、缺少分号、空指针异常、类型不匹配、逻辑错误、死循环、内存泄漏）

---

## 2026-09-07 第十四步：引入CodeMirror在线编辑器 + ECharts图表

提交作业页面：引入CodeMirror 6代码编辑器，支持语法高亮、行号、深色主题，支持Java和C++，上传文件+在线写代码两种模式

学习报告页面：引入ECharts 5.x图表库，饼图（错题类型分布）、折线图（学习趋势）、柱状图（知识点掌握）、时间切换、AI智能推荐

新增依赖：codemirror系列、echarts

---

## 2026-09-05 第十三步：UI风格统一为黑灰玻璃高级感

根本原因发现：main.js引入的是assets/global.css而非style.css，之前多次UI修改不生效就是因为改错了文件。

重写global.css为深色玻璃主题：
- 背景：深灰黑色（#0a0a0c）+ 暗紫渐变光晕
- 卡片：半透明深色背景 + 毛玻璃效果 + 半透明边框 + 大圆角
- 文字：主文字浅色（#f4f4f5），次文字灰色（#a1a1aa），强调色淡紫色（#c4b5fd）
- 覆盖Element Plus全局样式（按钮、输入框、下拉框、卡片、弹窗、分页、加载等）

---

## 2026-09-03 第十二步：首页优化

删除"核心功能"和"为什么选择EduCode"两块重复内容
首页加背景渐变光晕（右上角紫色、左下角蓝色）
Hero区域改成AI批改代码对比效果（左边错误代码红色标记、中间AI批改魔法棒图标、右边正确代码绿色标记）

---

## 2026-09-01 第十一步：项目改名 + 个人中心页面

项目改名：从ailearn改成EduCode，所有文档、页面标题、logo都统一

个人中心页面：用户基本信息展示、修改昵称、学习数据统计、账号安全设置

---

## 2026-08-28 第十步：学习报告页面

时间切换、数据概览卡片、错题类型分布饼图、学习趋势折线图、薄弱知识点列表、AI智能学习建议

---

## 2026-08-26 第九步：错题本页面

错题列表（卡片式）、筛选功能（全部/未掌握/已掌握，按错误类型筛选）、错题详情弹窗、标记已掌握功能、AI生成类似题入口

---

## 2026-08-25 第八步：提交作业页面

上传文件模式（支持.cpp/.java）、在线写代码模式、语言选择（C++/Java）、提交按钮+加载状态、批改结果展示

---
## 2026-08-23 第七步：登录/注册页视觉重设计（参赛版）

去掉"AI 味太重"的模板化特效（深紫渐变 + 粒子网络 + 发光能量球 + 暗色毛玻璃），改为**有产品灵魂的编辑风格设计**：

- 左侧品牌叙事区（深靛蓝 #1e1b4b + 细网格纹理）：
  - logo + EduCode 品牌名、大标语"让每一次学习都更高效"
  - 三个功能亮点（AI 智能批改 / 错题本 / 学习报告，配图标）
  - **代码窗口签名元素**：带 macOS 风格三色圆点的代码卡片，Python 代码语法高亮 + 闪烁光标，右下角浮出"AI 批改通过 · 95 分"标签——把产品的核心功能（AI 批代码）直接画在页面上
- 右侧表单区（浅灰底 + 纯白卡片，去掉毛玻璃）：
  - 品牌主色统一为靛蓝 #4F46E5，通过 CSS 变量覆盖 Element Plus 主题色，按钮/输入框焦点/下拉框/Tab 下划线全部自动跟随，形成统一的设计系统感
  - 按钮加悬停微动效（上浮 + 阴影）
- 删除了粒子 canvas 的整套 JS（约 100 行），页面更轻、代码更简洁
- 登录/注册功能逻辑完全没动
- 响应式：<900px 隐藏左侧品牌区，手机端表单占满
- 微调：整页加 20px 留白和稍深的底色，左右两栏分别加圆角（左 20px 左上/左下、右 20px 右上/右下），整体像一张悬浮的大卡片；窄屏时右栏变完整圆角
- 验证：`npm run build` 构建通过

---

## 2026-08-23 第六步：登录页改为登录/注册 Tab 切换

- `LoginView.vue` 顶部加 `el-tabs`，两个标签"登录"和"注册"（stretch 等宽），共用同一个毛玻璃卡片
- 登录 Tab：保持原有邮箱 + 密码表单和逻辑
- 注册 Tab：邮箱、用户名、密码（8-16 位）、确认密码（自定义校验两次一致）、角色下拉（学员/老师，默认学员）
- `src/api/user.js` 新增 `registerApi(email, password, username, role)`，调用 `POST /user/register`
- 注册成功：提示"注册成功，请登录"→ 自动切回登录 Tab → 把邮箱带进登录表单 → 清空注册表单
- 注册失败：409 翻译为"该邮箱/用户名已被注册"（后端返回英文 detail），其他错误显示后端信息
- `/register` 路由本来就不存在，无需删除
- 验证：`npm run build` 构建通过

---

## 2026-08-23 第五步：登录页对接真实后端接口

把模拟登录换成真实的后端登录（`POST /login`，参数 `{ email, password }`，成功返回 `{ token }`）：

- 新建 `src/api/user.js`：集中存放用户相关接口，导出 `loginApi(email, password)`
- `vite.config.js` 加了开发代理：前端请求 `/api/xxx` 转发到 `http://localhost:8080`（后端没有配 CORS，用代理解决跨域），`request.js` 的 baseURL 改回 `/api`
- `request.js`：响应拦截器不再统一弹错误提示，改为由页面自己弹（避免重复弹窗）
- `stores/user.js`：字段 `username` 改为 `email`，登录成功后保存 token 和邮箱；`MainLayout.vue` 导航栏同步显示邮箱
- `LoginView.vue`：
  - 表单改为邮箱 + 密码，加了邮箱格式校验
  - 调用 `loginApi` 发真实请求，`loading` 用 try/catch/finally 保证请求结束一定恢复
  - 失败提示：401 显示"邮箱或密码错误"（后端 401 时响应体为空），其他错误优先显示后端返回的信息
- 验证：`npm run build` 通过；开发服务器冒烟测试确认代理转发正常（后端未启动时返回 502）

---

## 2026-08-23 第四步：登录页改为整屏背景图 + 毛玻璃卡片

- 背景图铺满整个屏幕（不再左右分栏）
- 盖一层从左到右逐渐透明的白色渐变遮罩：左边清晰（保证表单可读），右边完全露出原图
- 登录卡片移到左侧、垂直居中，做成半透明"毛玻璃"效果：背景虚化（backdrop-filter: blur）、半透明边框、柔和阴影、圆角
- 右下角加了品牌标语（EduCode 智能学习平台），白色文字配阴影
- 登录功能代码（`<script setup>` 部分）依然没动
- 验证：`npm run build` 构建通过

---

## 2026-08-23 第三步：登录页改为左右两栏布局

把登录页从"表单居中"改为"左右两栏"：

- 左半部分：放宣传图片（`src/assets/login-bg.jpg`，来自 Unsplash 摄影师 steve-a-johnson，从 OneDrive 图片库复制进项目）
- 右半部分：登录表单保持居中，保留原来的蓝紫渐变背景
- 登录功能代码（`<script setup>` 部分）完全没动
- 增加了一个小屏幕适配：屏幕宽度小于 768px 时自动隐藏左边图片，只显示表单
- 验证：`npm run build` 构建通过

---

## 2026-08-23 第二步：清理 Vite 模板遗留文件

清理了脚手架自带的默认内容，换成项目自己的：

- `README.md`：原来是 Vite 模板的英文说明，已替换为项目介绍（启动方法 + 目录结构）
- `src/components/`：HelloWorld 组件删除后留下的空目录，已删除
- `dist/`：验证构建时生成的产物（已被 .gitignore 忽略），已删除
- HelloWorld 组件、Vite 默认图片（vue.svg、vite.svg 等）和模板默认样式：此步之前已删除/覆盖，无需再处理
- 保留：`.vscode/extensions.json`（推荐 Volar 插件，对 Vue 开发有用）

---

## 2026-08-23 第一步：搭建前端项目基础框架

### 一、这一步做了什么

1. 梳理了项目现状：`package.json` 中**已安装好全部所需依赖**（Vue 3、Vue Router、Pinia、Axios、Element Plus），因此本次**没有新安装任何依赖**。
2. 创建了完整的目录结构和基础代码框架：
   - 配置好 **Vue Router** 路由（含登录页、首页、404 页，以及带导航栏的主布局）
   - 配置好 **Pinia** 状态管理（用户登录信息 store，演示了登录 → 保存 → 退出的完整流程）
   - 配置好 **Axios** 请求封装（统一请求地址、自动带 token、统一错误提示）
   - 配置好 **Element Plus** 注册（组件库 + 全部图标全局注册）
3. 所有代码均为 **JavaScript**（未使用 TypeScript），并写了**大量中文注释**，注释面向零基础小白。
4. 验证：`npm run build` 构建通过，项目可正常编译。

### 二、项目使用的依赖（均在 package.json 中，无需再安装）

| 依赖 | 版本 | 作用 |
| ---- | ---- | ---- |
| vue | ^3.5.40 | Vue 3 框架本身 |
| vue-router | ^5.2.0 | 路由：管理"网址 ↔ 页面"的对应关系 |
| pinia | ^4.0.3 | 状态管理：保存全局共享的数据（如登录状态） |
| axios | ^1.19.0 | 网络请求：向后端发请求、拿数据 |
| element-plus | ^2.14.5 | UI 组件库：按钮、表单、卡片等现成组件 |
| @element-plus/icons-vue | ^2.3.2 | Element Plus 的图标库 |
| vite | ^8.2.0 | 构建工具：启动开发服务器、打包项目 |
| @vitejs/plugin-vue | ^6.0.8 | Vite 的 Vue 插件（让 Vite 认识 .vue 文件） |

### 三、目录结构和每个关键文件的作用

```
web/                          ← 前端项目根目录（本文件所在位置）
├── AI-开发日志.md              ← 本文件：开发记录
├── index.html                 ← 网页的"外壳"，浏览器最先加载它
├── package.json               ← 依赖清单和启动命令（npm 脚本）
├── vite.config.js             ← Vite 构建工具的配置
├── public/                    ← 静态资源：原样复制到网站的根目录
│   └── favicon.png            ← 浏览器标签页上的小图标
└── src/                       ← 源代码目录（我们写的代码都在这）
    ├── main.js                ← 入口文件：创建应用 + 安装 Pinia/Router/Element Plus
    ├── App.vue                ← 根组件：整个应用的"外壳"，内部只有 <router-view>
    ├── style.css              ← 全局样式：对整站生效的基础样式
    ├── assets/                ← 需要"打包处理"的静态资源（如图片）
    │   └── logo.png           ← 网站 logo
    ├── router/
    │   └── index.js           ← 路由表：定义网址和页面的对应关系 + 页面标题守卫
    ├── stores/
    │   └── user.js            ← Pinia store：保存登录 token、用户名，提供登录/退出方法
    ├── utils/
    │   └── request.js         ← Axios 封装：统一请求地址、自动带 token、统一错误提示
    ├── layout/
    │   └── MainLayout.vue     ← 主布局：顶部导航栏 + 主体区域（首页等都显示在里面）
    └── views/                 ← 页面文件夹（以后所有页面都放在这里）
        ├── HomeView.vue       ← 首页：欢迎信息和功能占位卡片
        ├── LoginView.vue      ← 登录页：表单 + 校验 + 模拟登录（后端好了换成真实请求）
        └── NotFoundView.vue   ← 404 页：访问不存在的网址时显示
```

### 四、路由规划（当前）

| 网址 | 页面 | 说明 |
| ---- | ---- | ---- |
| `/login` | LoginView | 登录页（独立页面，不带导航栏） |
| `/` | MainLayout → HomeView | 首页（套在带导航栏的主布局里） |
| 其他任何网址 | NotFoundView | 404 兜底页 |

### 五、几个重要的"约定"（以后开发要遵守）

1. **新页面**放在 `src/views/` 里，然后在 `src/router/index.js` 的路由表里加一条记录。
2. **全局共享的数据**（多个页面都要用）放进 `src/stores/` 的 store；只在一个页面用的数据写在该页面自己的 `<script setup>` 里。
3. **发网络请求**统一用 `src/utils/request.js` 导出的 `request`，不要直接用 axios。
4. 页面专属样式写在 `<style scoped>` 里；全站通用的样式写在 `src/style.css` 里。

### 六、如何启动项目

```bash
# 1. 进入前端项目目录（如果还没在 web 目录里）
cd web

# 2. 启动开发服务器（会自动打开/提示网址，默认 http://localhost:5173）
npm run dev
```

其他常用命令：

```bash
npm run build     # 打包项目，产物在 dist/ 文件夹（发布上线时用）
npm run preview   # 预览打包后的产物（先 build 再 preview）
```

### 七、下一步计划（待办）

- [ ] 等后端登录接口做好后，把 `LoginView.vue` 里的模拟登录换成真实请求（代码里已留好注释）
- [ ] 开发"我的课程"等业务页面
- [ ] 在 `router/index.js` 的全局守卫里加"未登录自动跳登录页"的检查
- [ ] 如需连后端，在 `vite.config.js` 里配置代理（把 /api 请求转发到后端端口）

